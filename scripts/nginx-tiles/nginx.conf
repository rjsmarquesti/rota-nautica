server {
    listen 80;
    server_name _;

    location / {
        auth_basic "Rota Nautica - teste pessoal";
        auth_basic_user_file /etc/nginx/.htpasswd;

        root /usr/share/nginx/html;
        autoindex off;

        add_header Cache-Control "private, max-age=86400";
    }
}
